import React from 'react';
import HeroCarousel from '../HeroCarousel/HeroCarousel';

const Home = () => {
    return (
        <div>
            <HeroCarousel></HeroCarousel>
        </div>
    );
};

export default Home;